#language: node_js
language: python

#node_js:
#  - 0.10

python:
 - 2.7
  
install: true

script:
  - echo "Inside script"
  - exit 1

after_script:
  - echo "This must run"
  - foobar
  
after_success:
  - echo "Success!"
  
after_failure:
  - echo "Failure :("
